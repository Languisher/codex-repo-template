# Project-scoped Codex defaults. Codex loads this file only after the project is
# trusted by the user.

sandbox_mode = "workspace-write"
approval_policy = "on-request"
model_reasoning_effort = "medium"
model_verbosity = "low"

# Keep overlay guidance available after embedding into a larger host project.
project_doc_max_bytes = 65536
